---
phase: 01-spec-generation-cli-and-project-scaffolding
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - .github/workflows/ci.yml
  - scripts/sync_tableau_schemas.py
  - examples/specs/finance-reconciliation.dashboard_spec.yaml
  - examples/specs/executive-kpi.dashboard_spec.yaml
  - examples/specs/ops-monitoring.dashboard_spec.yaml
  - examples/sql/finance_reconciliation.sql
  - tests/integration/test_examples.py
  - .pre-commit-config.yaml
autonomous: true
requirements:
  - PROJ-02
  - PROJ-03
  - PROJ-05

must_haves:
  truths:
    - "GitHub Actions CI workflow runs ruff lint, mypy type check, and pytest on Python 3.12 and 3.13"
    - "Example spec YAML files exist and load successfully through load_spec without validation errors"
    - "Finance reconciliation example spec generates a .twb through the full pipeline"
    - "Test infrastructure exists with unit and integration test directories populated"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with lint, type check, test matrix"
      contains: "ruff"
    - path: "examples/specs/finance-reconciliation.dashboard_spec.yaml"
      provides: "Finance reconciliation example spec"
      contains: "workbook"
    - path: "examples/specs/executive-kpi.dashboard_spec.yaml"
      provides: "Executive KPI example spec"
      contains: "workbook"
    - path: "examples/specs/ops-monitoring.dashboard_spec.yaml"
      provides: "Ops monitoring example spec"
      contains: "workbook"
    - path: "examples/sql/finance_reconciliation.sql"
      provides: "Example SQL for finance reconciliation datasource"
      contains: "SELECT"
    - path: "scripts/sync_tableau_schemas.py"
      provides: "Script to refresh vendored XSDs from upstream"
      contains: "sync"
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit hook configuration"
      contains: "ruff"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "references dev dependencies and pytest config"
      pattern: "pip install -e"
    - from: "examples/specs/"
      to: "src/tableau_agent_toolkit/spec/io.py"
      via: "example specs must load through load_spec"
      pattern: "load_spec"
    - from: "tests/integration/test_examples.py"
      to: "examples/specs/"
      via: "loads and validates each example spec"
      pattern: "examples/specs"
---

<objective>
Create the CI pipeline, example specs, XSD sync script, and pre-commit hooks. This completes the project infrastructure and provides the example content needed for the Phase 1 success criterion: "Example specs exist in the repository and generate successfully through the full spec-to-twb pipeline."

Purpose: CI ensures code quality gates are enforced. Example specs validate that the spec schema works for real-world use cases and serve as documentation. The sync script enables XSD updates from upstream.

Output: GitHub Actions CI workflow, three example specs with SQL, XSD sync script, and pre-commit hooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spec-generation-cli-and-project-scaffolding/01-RESEARCH.md
@.planning/phases/01-spec-generation-cli-and-project-scaffolding/01-01-SUMMARY.md
@.planning/phases/01-spec-generation-cli-and-project-scaffolding/01-04-SUMMARY.md
</context>

<interfaces>
<!-- Interfaces consumed from prior plans -->

From src/tableau_agent_toolkit/spec/io.py (Plan 01):
```python
def load_spec(path: Path) -> DashboardSpec: ...
def dump_spec(spec: DashboardSpec, path: Path) -> None: ...
```

From src/tableau_agent_toolkit/spec/models.py (Plan 01):
```python
class DashboardSpec(BaseModel):
    spec_version: str
    workbook: WorkbookSpec
    datasources: list[DatasourceSpec]
    parameters: list[ParameterSpec]
    calculations: list[CalculationSpec]
    worksheets: list[WorksheetSpec]
    dashboards: list[dict]
    publish: dict | None
    qa: dict | None

class DatasourceSpec(BaseModel):
    name: str
    mode: str
    connection: dict | None
    custom_sql_file: str | None
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CI workflow, pre-commit hooks, and XSD sync script</name>
  <files>
    .github/workflows/ci.yml,
    .pre-commit-config.yaml,
    scripts/sync_tableau_schemas.py
  </files>
  <read_first>
    - .planning/phases/01-spec-generation-cli-and-project-scaffolding/01-RESEARCH.md (CI workflow pattern at lines 145-153)
    - pyproject.toml (dev dependencies and tool configs)
  </read_first>
  <action>
Create .github/workflows/ci.yml with:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-type:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -e ".[dev]"
      - name: Lint with ruff
        run: ruff check .
      - name: Type check with mypy
        run: mypy src

  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]
        os: [ubuntu-latest, windows-latest]
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: pip install -e ".[dev]"
      - name: Run unit tests
        run: python -m pytest tests/unit -x -q
      - name: Run integration tests
        run: python -m pytest tests/integration -x -q
      - name: Coverage report
        run: python -m pytest --cov=tableau_agent_toolkit --cov-report=term-missing
```

Create .pre-commit-config.yaml with:

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v2.0.0
    hooks:
      - id: mypy
        additional_dependencies: [pydantic>=2.13, types-PyYAML]
```

Create scripts/sync_tableau_schemas.py with a script that:
1. Defines the upstream URL: "https://raw.githubusercontent.com/tableau/tableau-document-schemas/main/schemas/"
2. Defines target directory: "third_party/tableau_document_schemas/schemas/"
3. Defines versions to sync: ["2026_1"]
4. For each version, downloads the corresponding XSD file (e.g., "2026_1/twb_2026.1.0.xsd")
5. Writes files to target directory, creating subdirectories as needed
6. Updates the README.md with sync date
7. Uses urllib.request for downloads (no external dependency)
8. Has a `main()` function called from `if __name__ == "__main__"`
9. Uses argparse with a `--versions` argument to specify which versions to sync
10. Prints "Synced X files to third_party/" on success
  </action>
  <verify>
    <automated>cd "C:/Users/OSNuttapongB/Desktop/tableau-agent-toolkit" && test -f .github/workflows/ci.yml && test -f .pre-commit-config.yaml && test -f scripts/sync_tableau_schemas.py && python scripts/sync_tableau_schemas.py --help</automated>
  </verify>
  <done>
    - .github/workflows/ci.yml exists with lint, type-check, and test matrix (Python 3.12 + 3.13)
    - .pre-commit-config.yaml exists with ruff and mypy hooks
    - scripts/sync_tableau_schemas.py exists and runs with --help
  </done>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains `ruff check .`
    - .github/workflows/ci.yml contains `mypy src`
    - .github/workflows/ci.yml contains `python-version: "3.12"` and `python-version: "3.13"`
    - .github/workflows/ci.yml contains `--cov=tableau_agent_toolkit`
    - .pre-commit-config.yaml contains `ruff`
    - .pre-commit-config.yaml contains `mypy`
    - scripts/sync_tableau_schemas.py contains `def main`
    - scripts/sync_tableau_schemas.py contains `tableau-document-schemas`
    - python scripts/sync_tableau_schemas.py --help exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create example specs with SQL and integration tests</name>
  <files>
    examples/specs/finance-reconciliation.dashboard_spec.yaml,
    examples/specs/executive-kpi.dashboard_spec.yaml,
    examples/specs/ops-monitoring.dashboard_spec.yaml,
    examples/sql/finance_reconciliation.sql,
    tests/integration/test_examples.py
  </files>
  <read_first>
    - src/tableau_agent_toolkit/spec/models.py (model structure specs must conform to)
    - src/tableau_agent_toolkit/spec/io.py (load_spec function specs must pass through)
    - src/tableau_agent_toolkit/twb/generator.py (generate function specs must produce output from)
    - .planning/phases/01-spec-generation-cli-and-project-scaffolding/01-RESEARCH.md (example spec design at line 39)
  </read_first>
  <behavior>
    - Test 1: load_spec("examples/specs/finance-reconciliation.dashboard_spec.yaml") succeeds without error
    - Test 2: load_spec("examples/specs/executive-kpi.dashboard_spec.yaml") succeeds without error
    - Test 3: load_spec("examples/specs/ops-monitoring.dashboard_spec.yaml") succeeds without error
    - Test 4: Finance spec has at least 1 datasource with name containing "Finance" or "finance" or "reconciliation"
    - Test 5: Finance spec has at least 1 worksheet
    - Test 6: Finance spec has at least 1 dashboard
    - Test 7: Finance spec generates a .twb file via WorkbookGenerator (using minimal_template fixture)
  </behavior>
  <action>
Create directory structure:
- examples/specs/
- examples/sql/

Create examples/specs/finance-reconciliation.dashboard_spec.yaml with a complete finance reconciliation dashboard spec:
```yaml
spec_version: "1.0"
workbook:
  name: "Finance Reconciliation Dashboard"
  target_tableau_version: "2026.1"
  packaging: "twb"
  template:
    id: "finance-reconciliation"
    path: "finance/reconciliation_template.twb"
    required_anchors:
      - "datasource-pane"
      - "dashboard-layout"
datasources:
  - name: "Finance_GL"
    mode: "custom-sql"
    custom_sql_file: "sql/finance_reconciliation.sql"
    connection:
      type: "sqlserver"
      server: "${DB_HOST}"
      database: "${DB_NAME}"
  - name: "Finance_Subledger"
    mode: "embedded-extract"
    connection:
      type: "sqlserver"
      server: "${DB_HOST}"
      database: "${DB_NAME}"
parameters:
  - name: "ReconciliationPeriod"
    data_type: "string"
    default_value: "Current Month"
  - name: "VarianceThreshold"
    data_type: "float"
    default_value: "0.05"
calculations:
  - name: "Variance Amount"
    formula: "SUM([GL Balance]) - SUM([Subledger Balance])"
    comment: "Difference between GL and subledger"
  - name: "Variance Pct"
    formula: "ABS([Variance Amount]) / NULLIF(SUM([GL Balance]), 0)"
    comment: "Variance as percentage of GL balance"
worksheets:
  - name: "GL vs Subledger Comparison"
    datasource: "Finance_GL"
  - name: "Variance Analysis"
    datasource: "Finance_GL"
  - name: "Reconciliation Summary"
    datasource: "Finance_GL"
dashboards:
  - name: "Finance Reconciliation"
    layout: "tiled"
    sheets:
      - "GL vs Subledger Comparison"
      - "Variance Analysis"
      - "Reconciliation Summary"
```

Create examples/specs/executive-kpi.dashboard_spec.yaml with a simpler executive KPI spec:
```yaml
spec_version: "1.0"
workbook:
  name: "Executive KPI Dashboard"
  target_tableau_version: "2026.1"
  packaging: "twb"
  template:
    id: "finance-reconciliation"
    path: "finance/reconciliation_template.twb"
    required_anchors:
      - "datasource-pane"
      - "dashboard-layout"
datasources:
  - name: "KPI_Data"
    mode: "embedded-live"
    connection:
      type: "postgres"
      server: "${DB_HOST}"
      database: "${DB_NAME}"
calculations:
  - name: "Revenue Growth"
    formula: "(SUM([Current Revenue]) - SUM([Prior Revenue])) / NULLIF(SUM([Prior Revenue]), 0)"
worksheets:
  - name: "Revenue Trend"
    datasource: "KPI_Data"
  - name: "KPI Scorecard"
    datasource: "KPI_Data"
dashboards:
  - name: "Executive KPI"
    layout: "tiled"
    sheets:
      - "Revenue Trend"
      - "KPI Scorecard"
```

Create examples/specs/ops-monitoring.dashboard_spec.yaml with an ops monitoring spec:
```yaml
spec_version: "1.0"
workbook:
  name: "Operations Monitoring"
  target_tableau_version: "2026.1"
  packaging: "twb"
  template:
    id: "finance-reconciliation"
    path: "finance/reconciliation_template.twb"
    required_anchors:
      - "datasource-pane"
      - "dashboard-layout"
datasources:
  - name: "OpsMetrics"
    mode: "embedded-live"
    connection:
      type: "postgres"
      server: "${DB_HOST}"
      database: "${DB_NAME}"
  - name: "IncidentData"
    mode: "embedded-extract"
    connection:
      type: "sqlserver"
      server: "${DB_HOST}"
      database: "${DB_NAME}"
parameters:
  - name: "TimeRange"
    data_type: "string"
    default_value: "Last 24 Hours"
worksheets:
  - name: "System Health"
    datasource: "OpsMetrics"
  - name: "Incident Tracker"
    datasource: "IncidentData"
dashboards:
  - name: "Operations Dashboard"
    layout: "tiled"
    sheets:
      - "System Health"
      - "Incident Tracker"
```

Create examples/sql/finance_reconciliation.sql with a sample SQL query:
```sql
-- Finance Reconciliation Query
-- Compares GL balances with subledger balances for reconciliation
SELECT
    gl.account_number,
    gl.account_name,
    gl.balance AS gl_balance,
    sl.balance AS subledger_balance,
    gl.balance - sl.balance AS variance_amount,
    CASE
        WHEN gl.balance = 0 THEN NULL
        ELSE ABS(gl.balance - sl.balance) / ABS(gl.balance)
    END AS variance_pct,
    gl.period,
    gl.fiscal_year
FROM general_ledger gl
FULL OUTER JOIN subledger sl
    ON gl.account_number = sl.account_number
    AND gl.period = sl.period
    AND gl.fiscal_year = sl.fiscal_year
WHERE gl.period = DATE_TRUNC('month', CURRENT_DATE)
ORDER BY ABS(gl.balance - sl.balance) DESC;
```

Create tests/integration/test_examples.py with:
1. Test each spec loads through load_spec without error
2. Test finance spec has expected datasources, calculations, worksheets, dashboards
3. Test finance spec generates a .twb file using WorkbookGenerator with the minimal_template fixture (using template_override to bypass registry)
  </action>
  <verify>
    <automated>cd "C:/Users/OSNuttapongB/Desktop/tableau-agent-toolkit" && python -m pytest tests/integration/test_examples.py -x -v</automated>
  </verify>
  <done>
    - Three example specs exist and load successfully through load_spec
    - Finance reconciliation spec has datasources, calculations, worksheets, dashboards
    - Finance reconciliation SQL file exists
    - Finance spec generates a .twb through the full pipeline
    - All 7 integration tests pass
  </done>
  <acceptance_criteria>
    - examples/specs/finance-reconciliation.dashboard_spec.yaml contains "workbook"
    - examples/specs/finance-reconciliation.dashboard_spec.yaml contains "datasources"
    - examples/specs/finance-reconciliation.dashboard_spec.yaml contains "calculations"
    - examples/specs/finance-reconciliation.dashboard_spec.yaml contains "worksheets"
    - examples/specs/finance-reconciliation.dashboard_spec.yaml contains "dashboards"
    - examples/specs/executive-kpi.dashboard_spec.yaml contains "workbook"
    - examples/specs/ops-monitoring.dashboard_spec.yaml contains "workbook"
    - examples/sql/finance_reconciliation.sql contains "SELECT"
    - tests/integration/test_examples.py contains at least 7 test functions
    - python -m pytest tests/integration/test_examples.py -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> GitHub Actions | External service executing code; supply chain trust |
| scripts/sync_tableau_schemas.py -> GitHub | Downloads XSD files from external GitHub repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | scripts/sync_tableau_schemas.py | accept | Downloads from official tableau/tableau-document-schemas repo; low risk |
| T-01-13 | Information Disclosure | example specs | accept | Example specs use ${DB_HOST} placeholders, not real credentials |
| T-01-14 | Tampering | .github/workflows/ci.yml | accept | Standard CI workflow; pinned action versions (v4, v5) |
</threat_model>

<verification>
- `.github/workflows/ci.yml` exists with lint, type-check, and test steps
- `python -m pytest tests/integration/test_examples.py -x -v` passes all tests
- All three example specs load through load_spec without error
- `python scripts/sync_tableau_schemas.py --help` exits 0
</verification>

<success_criteria>
- CI workflow runs ruff, mypy, pytest on Python 3.12 and 3.13
- Three example specs (finance, KPI, ops) exist and validate
- Finance reconciliation spec generates through the full pipeline
- XSD sync script runs and has --help
- Pre-commit hooks configured for ruff and mypy
- All integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-spec-generation-cli-and-project-scaffolding/01-05-SUMMARY.md`
</output>
