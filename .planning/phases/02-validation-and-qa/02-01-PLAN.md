---
phase: 02-validation-and-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tableau_agent_toolkit/validation/report.py
  - src/tableau_agent_toolkit/qa/__init__.py
  - src/tableau_agent_toolkit/qa/report.py
  - tests/fixtures/broken_references.twb
  - tests/fixtures/valid_full.twb
  - tests/fixtures/empty_dashboard.twb
  - tests/fixtures/missing_calculation.twb
  - tests/unit/validation/test_report.py
  - tests/unit/qa/__init__.py
  - tests/unit/qa/test_report.py
autonomous: true
requirements: [VAL-03, QA-03]

must_haves:
  truths:
    - "Validation report types exist with SemanticIssue, Severity enum, and SemanticValidationResult"
    - "QA report types exist with QACheckResult, CheckStatus enum, and generate_qa_report function"
    - "Test fixtures exist for broken references, valid full TWB, empty dashboard, and missing calculation"
  artifacts:
    - path: "src/tableau_agent_toolkit/validation/report.py"
      provides: "SemanticIssue, Severity, SemanticValidationResult dataclasses"
      exports: ["SemanticIssue", "Severity", "SemanticValidationResult"]
    - path: "src/tableau_agent_toolkit/qa/__init__.py"
      provides: "QA module init"
    - path: "src/tableau_agent_toolkit/qa/report.py"
      provides: "QACheckResult, CheckStatus, generate_qa_report"
      exports: ["QACheckResult", "CheckStatus", "generate_qa_report"]
    - path: "tests/fixtures/broken_references.twb"
      provides: "TWB with broken sheet/datasource references for semantic validator testing"
      contains: "NonExistentSheet"
    - path: "tests/fixtures/valid_full.twb"
      provides: "TWB with all valid cross-references"
    - path: "tests/fixtures/empty_dashboard.twb"
      provides: "TWB with empty dashboard zones"
    - path: "tests/fixtures/missing_calculation.twb"
      provides: "TWB referencing non-existent calculation"
  key_links:
    - from: "src/tableau_agent_toolkit/validation/report.py"
      to: "src/tableau_agent_toolkit/validation/xsd.py"
      via: "follows same dataclass pattern (XsdError, XsdValidationResult)"
      pattern: "dataclass.*SemanticIssue|dataclass.*SemanticValidationResult"
---

<objective>
Create validation report types, QA report types, and TWB test fixtures that downstream plans (semantic validator, QA checker) depend on.

Purpose: Establish the data contracts and test data so Plans 02-04 can build implementation against typed interfaces without exploring the codebase.
Output: Report type modules, 4 TWB test fixtures, unit tests for report types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-validation-and-qa/02-RESEARCH.md

@src/tableau_agent_toolkit/validation/xsd.py
@src/tableau_agent_toolkit/validation/__init__.py
@src/tableau_agent_toolkit/spec/models.py
@tests/fixtures/minimal_template.twb
@tests/conftest.py

<interfaces>
<!-- Existing patterns from Phase 1 that this plan must follow -->

From src/tableau_agent_toolkit/validation/xsd.py:
```python
@dataclass
class XsdError:
    line: int
    column: int
    message: str

@dataclass
class XsdValidationResult:
    valid: bool
    errors: list[XsdError] = field(default_factory=list)

class XsdValidator:
    def __init__(self, schemas_root: Path) -> None: ...
    def validate(self, twb_path: Path, tableau_version: str = "2026.1") -> XsdValidationResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create validation report types and QA report types</name>
  <files>
    src/tableau_agent_toolkit/validation/report.py,
    src/tableau_agent_toolkit/qa/__init__.py,
    src/tableau_agent_toolkit/qa/report.py,
    tests/unit/validation/test_report.py,
    tests/unit/qa/__init__.py,
    tests/unit/qa/test_report.py
  </files>
  <read_first>
    src/tableau_agent_toolkit/validation/xsd.py
    src/tableau_agent_toolkit/validation/__init__.py
  </read_first>
  <behavior>
    Validation report (tests/unit/validation/test_report.py):
    - Test 1: SemanticIssue dataclass has severity (Severity enum), category (str), message (str), xml_element (lxml etree._Element | None), spec_ref (str | None)
    - Test 2: Severity enum has ERROR, WARNING, INFO values
    - Test 3: SemanticValidationResult dataclass has valid (bool), errors (list[SemanticIssue]), warnings (list[SemanticIssue])
    - Test 4: SemanticValidationResult.valid is True when errors list is empty

    QA report (tests/unit/qa/test_report.py):
    - Test 5: CheckStatus enum has PASS, FAIL, WARN, SKIP values
    - Test 6: QACheckResult dataclass has check_name (str), status (CheckStatus), message (str), details (list[str])
    - Test 7: generate_qa_report produces markdown starting with "# QA Report:" header
    - Test 8: generate_qa_report orders sections: Errors first, then Warnings, then Passed Checks
    - Test 9: generate_qa_report shows summary line with counts (e.g., "**Summary:** 2 passed, 1 warnings, 1 failed")
    - Test 10: generate_qa_report includes "Remediation:" text for failed checks
  </behavior>
  <action>
    Write tests first (RED), then implement the modules (GREEN).

    For src/tableau_agent_toolkit/validation/report.py:
    - Create a Severity enum (str, Enum) with values ERROR="error", WARNING="warning", INFO="info"
    - Create a SemanticIssue dataclass with fields: severity (Severity), category (str), message (str), xml_element (etree._Element | None = None), spec_ref (str | None = None)
    - Create a SemanticValidationResult dataclass with fields: valid (bool), errors (list[SemanticIssue] = field(default_factory=list)), warnings (list[SemanticIssue] = field(default_factory=list))
    - Import Severity, SemanticIssue, SemanticValidationResult in validation/__init__.py

    For src/tableau_agent_toolkit/qa/__init__.py:
    - Create empty init with docstring: """QA module for static quality checks on Tableau workbooks."""

    For src/tableau_agent_toolkit/qa/report.py:
    - Create a CheckStatus enum (str, Enum) with values PASS="PASS", FAIL="FAIL", WARN="WARN", SKIP="SKIP"
    - Create a QACheckResult dataclass with fields: check_name (str), status (CheckStatus), message (str), details (list[str] = field(default_factory=list))
    - Create generate_qa_report(results: list[QACheckResult], twb_path: Path) -> str function that:
      1. Writes header: "# QA Report: {twb_path.name}"
      2. Writes summary: "**Summary:** {n_passed} passed, {n_warned} warnings, {n_failed} failed"
      3. Writes "## Errors" section with each failed check: "- **{check_name}**: {message}", details as sub-bullets, and "Remediation: Fix the identified issue and re-validate."
      4. Writes "## Warnings" section with each warned check
      5. Writes "## Passed Checks" section with each passed check: "- {check_name}"
    - Use only f-strings and "\n".join() -- no template engine dependency
  </action>
  <verify>
    <automated>cd "C:/Users/OSNuttapongB/Desktop/tableau-agent-toolkit" && python -m pytest tests/unit/validation/test_report.py tests/unit/qa/test_report.py -x -q</automated>
  </verify>
  <done>
    - SemanticIssue, Severity, SemanticValidationResult defined in validation/report.py
    - CheckStatus, QACheckResult, generate_qa_report defined in qa/report.py
    - All 10 tests passing
    - Modules importable: python -c "from tableau_agent_toolkit.validation.report import SemanticIssue, Severity, SemanticValidationResult" succeeds
    - python -c "from tableau_agent_toolkit.qa.report import CheckStatus, QACheckResult, generate_qa_report" succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TWB test fixtures for semantic validation and QA testing</name>
  <files>
    tests/fixtures/broken_references.twb,
    tests/fixtures/valid_full.twb,
    tests/fixtures/empty_dashboard.twb,
    tests/fixtures/missing_calculation.twb
  </files>
  <read_first>
    tests/fixtures/minimal_template.twb
    src/tableau_agent_toolkit/twb/generator.py
    src/tableau_agent_toolkit/spec/models.py
  </read_first>
  <action>
    Create 4 TWB fixture files that exercise the semantic validator and QA checker. Each must be valid XML with the same root structure as minimal_template.twb (workbook element with version='26.1', xmlns:user namespace, document-format-change-manifest).

    tests/fixtures/broken_references.twb:
    - Has <datasources> with datasource name='Data' (inline='true') containing a <column name='Amount' datatype='float' />
    - Has <worksheets> with one worksheet name='Sheet1'
    - Has <dashboards> with one dashboard name='Dash1' containing <zones> with two zones:
      1. <zone name='Sheet1' /> (valid reference)
      2. <zone name='NonExistentSheet' /> (BROKEN -- no worksheet named NonExistentSheet exists)
    - Has <actions> with one <action> containing <source worksheet='MissingSheet' /> (BROKEN reference)

    tests/fixtures/valid_full.twb:
    - Has <datasources> with datasource name='SalesData' (inline='true') containing:
      1. <column name='Amount' datatype='float' role='measure' />
      2. <column name='Revenue' datatype='real' role='measure'><calculation formula='SUM([Amount])' class='tableau' /></column>
    - Has <worksheets> with two worksheets: name='SalesChart', name='RevenueTable'
    - Has <dashboards> with one dashboard name='Overview' containing <zones> with two zones referencing valid worksheets: name='SalesChart', name='RevenueTable'
    - All cross-references are valid

    tests/fixtures/empty_dashboard.twb:
    - Has <datasources> with datasource name='Data' (inline='true')
    - Has <worksheets> with one worksheet name='Sheet1'
    - Has <dashboards> with one dashboard name='EmptyDash' containing <zones /> (empty -- no zone children)
    - This fixture should trigger the "empty dashboard" QA check

    tests/fixtures/missing_calculation.twb:
    - Has <datasources> with datasource name='Data' (inline='true') containing:
      1. <column name='Amount' datatype='float' />
      2. <column name='Variance' datatype='real'><calculation formula='SUM([MissingField])' class='tableau' /></column>
    - Has <worksheets> with one worksheet name='Sheet1'
    - The formula references [MissingField] which has no matching <column> definition -- triggers dangling field reference check
  </action>
  <verify>
    <automated>cd "C:/Users/OSNuttapongB/Desktop/tableau-agent-toolkit" && python -c "from lxml import etree; [etree.parse(f'tests/fixtures/{f}') for f in ['broken_references.twb', 'valid_full.twb', 'empty_dashboard.twb', 'missing_calculation.twb']]; print('All 4 fixtures parse successfully')"</automated>
  </verify>
  <done>
    - 4 TWB fixture files exist in tests/fixtures/
    - Each file parses as valid XML via lxml
    - broken_references.twb contains zone with name='NonExistentSheet' and action source with worksheet='MissingSheet'
    - valid_full.twb contains valid cross-references between dashboards, worksheets, and datasources
    - empty_dashboard.twb contains dashboard with empty <zones />
    - missing_calculation.twb contains calculation formula referencing [MissingField] with no matching column
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates type definitions and test fixtures only -- no external input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | test fixtures | accept | Test fixtures are static data under version control; no untrusted input |
</threat_model>

<verification>
- All report type tests pass: `pytest tests/unit/validation/test_report.py tests/unit/qa/test_report.py -x -q`
- All 4 TWB fixtures parse as valid XML
- Modules importable without error
</verification>

<success_criteria>
- SemanticIssue, Severity, SemanticValidationResult defined and tested
- CheckStatus, QACheckResult, generate_qa_report defined and tested
- generate_qa_report produces markdown with correct section ordering (errors, warnings, passed)
- 4 TWB test fixtures exist and parse successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-and-qa/02-01-SUMMARY.md`
</output>
