---
phase: 01-spec-generation-cli-and-project-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tableau_agent_toolkit/twb/__init__.py
  - src/tableau_agent_toolkit/twb/manifest.py
  - src/tableau_agent_toolkit/templates/__init__.py
  - src/tableau_agent_toolkit/templates/registry.py
  - templates/catalog.yaml
  - tests/unit/twb/__init__.py
  - tests/unit/twb/test_manifest.py
  - tests/unit/templates/__init__.py
  - tests/unit/templates/test_registry.py
autonomous: true
requirements:
  - GEN-02
  - GEN-03
  - GEN-04

must_haves:
  truths:
    - "XSD filename twb_2026.1.0.xsd maps to TWB version string 26.1 via TableauVersion utility"
    - "XSD filename with invalid format raises clear ValueError"
    - "ManifestByVersion element is inserted correctly into workbook XML with correct version attributes"
    - "Template registry resolves a template ID to a template path from catalog.yaml"
    - "Template registry raises clear error for unknown template ID"
  artifacts:
    - path: "src/tableau_agent_toolkit/twb/manifest.py"
      provides: "TableauVersion dataclass and apply_manifest_by_version()"
      exports: ["TableauVersion", "apply_manifest_by_version"]
    - path: "src/tableau_agent_toolkit/templates/registry.py"
      provides: "TemplateRegistry class with resolve() method"
      exports: ["TemplateRegistry", "TemplateMatch"]
    - path: "templates/catalog.yaml"
      provides: "Template catalog mapping template IDs to file paths and versions"
      contains: "templates"
    - path: "tests/unit/twb/test_manifest.py"
      provides: "Unit tests for version mapping and manifest application"
    - path: "tests/unit/templates/test_registry.py"
      provides: "Unit tests for template registry resolution"
  key_links:
    - from: "src/tableau_agent_toolkit/twb/manifest.py"
      to: "src/tableau_agent_toolkit/spec/models.py"
      via: "version string parsing from WorkbookSpec.target_tableau_version (conceptual link, no import)"
      pattern: "target_tableau_version"
    - from: "src/tableau_agent_toolkit/templates/registry.py"
      to: "templates/catalog.yaml"
      via: "YAML catalog file loading on init"
      pattern: "catalog.yaml"
---

<objective>
Create the TWB version mapping utility (TableauVersion), manifest handler (apply_manifest_by_version), and template registry (TemplateRegistry with catalog.yaml). These are the intermediate layer between spec models and the generator.

Purpose: The version mapping utility prevents the critical pitfall of XSD filename / TWB version string mismatch. The template registry provides the lookup mechanism the generator needs. Both must exist before the generator can be built.

Output: TableauVersion utility, manifest handler, template registry with catalog.yaml, and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spec-generation-cli-and-project-scaffolding/01-RESEARCH.md
</context>

<interfaces>
<!-- No imports from Plan 01. These modules are self-contained. -->

<!-- Contracts created by this plan (consumed by later plans): -->

From src/tableau_agent_toolkit/twb/manifest.py:
```python
@dataclass(frozen=True)
class TableauVersion:
    major: int
    minor: int
    @classmethod
    def from_xsd_filename(cls, filename: str) -> "TableauVersion": ...
    @classmethod
    def from_display_version(cls, version: str) -> "TableauVersion": ...
    @property
    def twb_version_string(self) -> str: ...  # "26.1"
    @property
    def xsd_version_string(self) -> str: ...  # "2026.1.0"

def apply_manifest_by_version(tree: etree._ElementTree, tableau_version: str) -> None: ...
```

From src/tableau_agent_toolkit/templates/registry.py:
```python
@dataclass
class TemplateMatch:
    id: str
    path: Path
    required_anchors: list[str]
    tableau_versions: list[str]

class TemplateRegistry:
    def __init__(self, catalog_path: Path | None = None): ...
    def resolve(self, template_id: str, tableau_version: str | None = None) -> TemplateMatch: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TableauVersion utility and manifest handler</name>
  <files>
    src/tableau_agent_toolkit/twb/__init__.py,
    src/tableau_agent_toolkit/twb/manifest.py,
    tests/unit/twb/__init__.py,
    tests/unit/twb/test_manifest.py
  </files>
  <read_first>
    - .planning/phases/01-spec-generation-cli-and-project-scaffolding/01-RESEARCH.md (version mapping pattern at lines 243-271, manifest application at lines 430-457)
  </read_first>
  <behavior>
    - Test 1: TableauVersion.from_xsd_filename("twb_2026.1.0.xsd") returns TableauVersion(major=26, minor=1)
    - Test 2: TableauVersion(26, 1).twb_version_string returns "26.1"
    - Test 3: TableauVersion(26, 1).xsd_version_string returns "2026.1.0"
    - Test 4: TableauVersion.from_xsd_filename("invalid.xsd") raises ValueError
    - Test 5: TableauVersion.from_xsd_filename("twb_2025.4.0.xsd") returns TableauVersion(major=25, minor=4)
    - Test 6: apply_manifest_by_version sets root version attribute to "26.1" when given "2026.1"
    - Test 7: apply_manifest_by_version sets root original-version attribute to "26.1"
    - Test 8: apply_manifest_by_version creates document-format-change-manifest with ManifestByVersion child element
    - Test 9: apply_manifest_by_version preserves existing namespace declarations (xmlns:user)
    - Test 10: TableauVersion is frozen (immutable) -- attempting to set major raises AttributeError
  </behavior>
  <action>
Create directory structure:
- src/tableau_agent_toolkit/twb/ (with empty __init__.py)
- tests/unit/twb/ (with empty __init__.py)

Create tests/unit/twb/test_manifest.py with tests covering all 10 behaviors above.

Create src/tableau_agent_toolkit/twb/manifest.py with:

1. `TableauVersion` as a frozen dataclass with `major: int` and `minor: int` fields:
   - `from_xsd_filename(cls, filename: str) -> TableauVersion`: Parse "twb_2026.1.0.xsd" -> TableauVersion(26, 1). Use regex `r"twb_(\d{4})\.(\d+)\.\d+\.xsd"`. Raise ValueError("Invalid XSD filename: {filename}") on no match. Derive major = year - 2000, minor = release number.
   - `from_display_version(cls, version: str) -> TableauVersion`: Parse "2026.1" -> TableauVersion(26, 1). Use regex `r"(\d{4})\.(\d+)"`. Raise ValueError on no match.
   - `twb_version_string` property: Returns "26.1" for TableauVersion(26, 1)
   - `xsd_version_string` property: Returns "2026.1.0" for TableauVersion(26, 1)
   - Mark with `@dataclass(frozen=True)` for immutability.

2. `apply_manifest_by_version(tree: etree._ElementTree, tableau_version: str) -> None`:
   - Parse tableau_version (e.g., "2026.1") using TableauVersion.from_display_version()
   - Get root element, set root.attrib["version"] = version.twb_version_string
   - Set root.attrib["original-version"] = version.twb_version_string
   - Set root.attrib["source-build"] = "0.0.0 (0000.0.0.0.0)"
   - Set root.attrib["source-platform"] = "win"
   - Find or create `<document-format-change-manifest>` element under root
   - Clear existing children and append `etree.Element("ManifestByVersion")`
   - Do NOT remove or reorder existing namespace declarations on root element

Use `from lxml import etree` for XML operations.
  </action>
  <verify>
    <automated>cd "C:/Users/OSNuttapongB/Desktop/tableau-agent-toolkit" && python -m pytest tests/unit/twb/test_manifest.py -x -v</automated>
  </verify>
  <done>
    - TableauVersion correctly maps XSD filenames to TWB version strings
    - TableauVersion correctly maps display versions (2026.1) to TWB version strings (26.1)
    - apply_manifest_by_version sets all version attributes and creates ManifestByVersion element
    - Namespace declarations preserved during manifest application
    - All 10 tests pass
  </done>
  <acceptance_criteria>
    - src/tableau_agent_toolkit/twb/manifest.py contains `class TableauVersion`
    - src/tableau_agent_toolkit/twb/manifest.py contains `frozen=True`
    - src/tableau_agent_toolkit/twb/manifest.py contains `from_xsd_filename`
    - src/tableau_agent_toolkit/twb/manifest.py contains `from_display_version`
    - src/tableau_agent_toolkit/twb/manifest.py contains `twb_version_string`
    - src/tableau_agent_toolkit/twb/manifest.py contains `xsd_version_string`
    - src/tableau_agent_toolkit/twb/manifest.py contains `def apply_manifest_by_version`
    - src/tableau_agent_toolkit/twb/manifest.py contains `ManifestByVersion`
    - src/tableau_agent_toolkit/twb/manifest.py contains `source-build`
    - tests/unit/twb/test_manifest.py contains at least 10 test functions
    - python -m pytest tests/unit/twb/test_manifest.py -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create template registry with catalog.yaml</name>
  <files>
    src/tableau_agent_toolkit/templates/__init__.py,
    src/tableau_agent_toolkit/templates/registry.py,
    templates/catalog.yaml,
    tests/unit/templates/__init__.py,
    tests/unit/templates/test_registry.py
  </files>
  <read_first>
    - .planning/phases/01-spec-generation-cli-and-project-scaffolding/01-RESEARCH.md (template registry pattern at lines 163-183, catalog.yaml design)
  </read_first>
  <behavior>
    - Test 1: TemplateRegistry loads from catalog.yaml and resolves a known template ID to a TemplateMatch
    - Test 2: TemplateRegistry.resolve raises ValueError for unknown template ID
    - Test 3: TemplateMatch contains the correct path from catalog.yaml
    - Test 4: TemplateRegistry loads catalog from default path when no path specified
    - Test 5: TemplateRegistry raises FileNotFoundError when catalog.yaml does not exist
    - Test 6: TemplateMatch has required_anchors list populated from catalog
  </behavior>
  <action>
Create directory structure:
- src/tableau_agent_toolkit/templates/ (with empty __init__.py)
- tests/unit/templates/ (with empty __init__.py)
- templates/ directory at project root

Create templates/catalog.yaml with the following content:
```yaml
# Template catalog for tableau-agent-toolkit
# Each template entry maps a logical ID to a file path and Tableau version compatibility
templates:
  finance-reconciliation:
    path: "finance/reconciliation_template.twb"
    tableau_versions:
      - "2026.1"
    required_anchors:
      - "datasource-pane"
      - "dashboard-layout"
```

Create tests/unit/templates/test_registry.py with tests covering all 6 behaviors above. Use a temp directory with a test catalog.yaml for isolation.

Create src/tableau_agent_toolkit/templates/registry.py with:

1. `TemplateMatch` dataclass:
   - `id: str` -- template logical ID
   - `path: Path` -- resolved absolute path to the .twb template file
   - `required_anchors: list[str]` -- anchor elements the template expects
   - `tableau_versions: list[str]` -- compatible Tableau versions

2. `TemplateRegistry` class:
   - `__init__(self, catalog_path: Path | None = None)`:
     - Default catalog_path to `Path(__file__).parent.parent.parent.parent / "templates" / "catalog.yaml"` (project root templates/)
     - Load catalog using yaml.safe_load()
     - Store template entries internally
   - `resolve(self, template_id: str, tableau_version: str | None = None) -> TemplateMatch`:
     - Look up template_id in loaded catalog
     - If not found, raise ValueError(f"Template not found: {template_id}")
     - If tableau_version provided, verify it is in the template's tableau_versions list (or warn if not)
     - Resolve path relative to catalog file's parent directory
     - Return TemplateMatch with resolved absolute path

Use `yaml.safe_load()` exclusively. Do NOT use yaml.load().
  </action>
  <verify>
    <automated>cd "C:/Users/OSNuttapongB/Desktop/tableau-agent-toolkit" && python -m pytest tests/unit/templates/test_registry.py -x -v</automated>
  </verify>
  <done>
    - TemplateRegistry loads catalog.yaml and resolves template IDs to TemplateMatch objects
    - Unknown template ID raises clear ValueError
    - TemplateMatch contains resolved path, required_anchors, and compatible versions
    - catalog.yaml exists with at least one template entry
    - All 6 tests pass
  </done>
  <acceptance_criteria>
    - src/tableau_agent_toolkit/templates/registry.py contains `class TemplateRegistry`
    - src/tableau_agent_toolkit/templates/registry.py contains `class TemplateMatch`
    - src/tableau_agent_toolkit/templates/registry.py contains `def resolve`
    - src/tableau_agent_toolkit/templates/registry.py contains `yaml.safe_load`
    - templates/catalog.yaml contains `finance-reconciliation`
    - templates/catalog.yaml contains `required_anchors`
    - tests/unit/templates/test_registry.py contains at least 6 test functions
    - python -m pytest tests/unit/templates/test_registry.py -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog.yaml -> TemplateRegistry | Untrusted input from filesystem; path traversal risk in template paths |
| lxml tree manipulation | XML element modification during manifest application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | templates/registry.py:resolve | mitigate | Resolve template paths relative to catalog directory; reject paths containing ".." components |
| T-01-05 | Information Disclosure | twb/manifest.py | accept | No sensitive data in version strings or manifest attributes |
</threat_model>

<verification>
- `python -m pytest tests/unit/twb/test_manifest.py tests/unit/templates/test_registry.py -x -v` passes all tests
- `python -c "from tableau_agent_toolkit.twb.manifest import TableauVersion, apply_manifest_by_version"` succeeds
- `python -c "from tableau_agent_toolkit.templates.registry import TemplateRegistry"` succeeds
- templates/catalog.yaml exists with valid YAML
</verification>

<success_criteria>
- TableauVersion maps XSD filenames to TWB version strings correctly (26.1 not 2026.1 or 26.1.0)
- apply_manifest_by_version sets all version attributes and creates ManifestByVersion element
- TemplateRegistry resolves template IDs from catalog.yaml
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-spec-generation-cli-and-project-scaffolding/01-02-SUMMARY.md`
</output>
