---
phase: 02-validation-and-qa
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/tableau_agent_toolkit/cli.py
  - tests/integration/test_cli.py
autonomous: true
requirements: [VAL-02, VAL-03, QA-01, QA-03]

must_haves:
  truths:
    - "User can run validate-semantic --input workbook.twb and receive a report of cross-reference errors"
    - "User can run qa static --input workbook.twb and receive a markdown QA report"
    - "CLI commands appear in --help output"
    - "qa static --output report.md writes markdown to file"
  artifacts:
    - path: "src/tableau_agent_toolkit/cli.py"
      provides: "validate-semantic and qa static CLI commands"
      contains: "validate-semantic"
    - path: "tests/integration/test_cli.py"
      provides: "Integration tests for new CLI commands"
  key_links:
    - from: "src/tableau_agent_toolkit/cli.py"
      to: "src/tableau_agent_toolkit/validation/semantic.py"
      via: "imports and calls SemanticValidator.validate()"
      pattern: "from.*validation\.semantic import SemanticValidator"
    - from: "src/tableau_agent_toolkit/cli.py"
      to: "src/tableau_agent_toolkit/qa/checker.py"
      via: "imports and calls StaticQAChecker.check_all()"
      pattern: "from.*qa\.checker import StaticQAChecker"
    - from: "src/tableau_agent_toolkit/cli.py"
      to: "src/tableau_agent_toolkit/qa/report.py"
      via: "imports generate_qa_report"
      pattern: "from.*qa\.report import generate_qa_report"
---

<objective>
Add validate-semantic and qa static CLI commands to the existing Typer app, with integration tests.

Purpose: CLI-01 from Phase 1 defined the commands but validate-semantic and qa static were not implemented. This plan wires the SemanticValidator and StaticQAChecker into the CLI so users can invoke them from the command line.
Output: Two new CLI commands, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-validation-and-qa/02-RESEARCH.md
@.planning/phases/02-validation-and-qa/02-02-SUMMARY.md
@.planning/phases/02-validation-and-qa/02-03-SUMMARY.md

@src/tableau_agent_toolkit/cli.py
@tests/integration/test_cli.py

<interfaces>
<!-- From Plan 02 (SemanticValidator) -->

From src/tableau_agent_toolkit/validation/semantic.py:
```python
class SemanticValidator:
    def validate(self, twb_path: Path) -> SemanticValidationResult: ...
```

From src/tableau_agent_toolkit/validation/report.py:
```python
@dataclass
class SemanticValidationResult:
    valid: bool
    errors: list[SemanticIssue] = field(default_factory=list)
    warnings: list[SemanticIssue] = field(default_factory=list)

@dataclass
class SemanticIssue:
    severity: Severity
    category: str
    message: str
    xml_element: etree._Element | None = None
    spec_ref: str | None = None
```

<!-- From Plan 03 (StaticQAChecker) -->

From src/tableau_agent_toolkit/qa/checker.py:
```python
class StaticQAChecker:
    def check_all(self, twb_path: Path) -> list[QACheckResult]: ...
```

From src/tableau_agent_toolkit/qa/report.py:
```python
def generate_qa_report(results: list[QACheckResult], twb_path: Path) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validate-semantic and qa static commands to CLI with integration tests</name>
  <files>
    src/tableau_agent_toolkit/cli.py,
    tests/integration/test_cli.py
  </files>
  <read_first>
    src/tableau_agent_toolkit/cli.py
    tests/integration/test_cli.py
    src/tableau_agent_toolkit/validation/semantic.py
    src/tableau_agent_toolkit/qa/checker.py
    src/tableau_agent_toolkit/qa/report.py
  </read_first>
  <behavior>
    CLI integration tests (add to tests/integration/test_cli.py):
    - Test 1: "validate-semantic --help" exits 0 and shows help text
    - Test 2: "validate-semantic --help" shows "input" or "TWB" argument
    - Test 3: "validate-semantic" on nonexistent file exits non-zero
    - Test 4: "validate-semantic" on valid_full.twb fixture exits 0 and prints "Valid" or "passes"
    - Test 5: "validate-semantic" on broken_references.twb fixture exits 1 and prints error text
    - Test 6: "qa --help" exits 0 and shows "static" subcommand
    - Test 7: "qa static --help" exits 0 and shows "--output" option
    - Test 8: "qa static" on valid_full.twb prints markdown with "# QA Report"
    - Test 9: "qa static --output report.md" on valid_full.twb writes file to specified path
    - Test 10: "--help" output shows "validate-semantic" command
    - Test 11: "--help" output shows "qa" subcommand group
  </behavior>
  <action>
    Write tests first (RED), then implement CLI commands (GREEN).

    Add to src/tableau_agent_toolkit/cli.py:

    1. Add imports at top:
       from tableau_agent_toolkit.validation.semantic import SemanticValidator
       from tableau_agent_toolkit.qa.checker import StaticQAChecker
       from tableau_agent_toolkit.qa.report import generate_qa_report

    2. Add validate-semantic command:
       @app.command("validate-semantic")
       def validate_semantic(
           twb_path: Path = typer.Argument(..., help="Path to .twb file", exists=True),
           spec_path: Path | None = typer.Option(None, "--spec", help="Path to original spec for error mapping"),
       ) -> None:
           """Validate TWB cross-references (sheet refs, calc names, action targets, field refs)."""
           validator = SemanticValidator()
           result = validator.validate(twb_path)
           if result.valid:
               typer.echo(f"Valid: {twb_path} passes semantic validation")
           else:
               typer.echo(f"Invalid: {twb_path} failed semantic validation", err=True)
               for err in result.errors:
                   typer.echo(f"  ERROR: {err.message}", err=True)
               for warn in result.warnings:
                   typer.echo(f"  WARNING: {warn.message}", err=True)
               sys.exit(1)

    3. Add qa subcommand group and static command:
       qa_app = typer.Typer(help="QA operations")
       app.add_typer(qa_app, name="qa")

       @qa_app.command("static")
       def qa_static(
           twb_path: Path = typer.Argument(..., help="Path to .twb file", exists=True),
           output: Path | None = typer.Option(None, "--output", "-o", help="Output markdown report path"),
       ) -> None:
           """Run static QA checks and generate report."""
           checker = StaticQAChecker()
           results = checker.check_all(twb_path)
           report = generate_qa_report(results, twb_path)
           if output:
               output.parent.mkdir(parents=True, exist_ok=True)
               output.write_text(report, encoding="utf-8")
               typer.echo(f"QA report written to {output}")
           else:
               typer.echo(report)

           # Exit non-zero if any check failed
           if any(r.status.value == "FAIL" for r in results):
               sys.exit(1)

    For tests/integration/test_cli.py, add new test classes:

    class TestValidateSemanticCommand:
       - Test --help, argument discovery, docstring, valid TWB (exit 0), broken TWB (exit 1 with error text)

    class TestQaStaticCommand:
       - Test qa --help, qa static --help, --output option, valid TWB output, --output writes file

    class TestHelpDiscovery:
       - Test top-level --help shows validate-semantic and qa

    Use FIXTURES_DIR = Path(__file__).parent.parent / "fixtures" to reference test fixtures.
  </action>
  <verify>
    <automated>cd "C:/Users/OSNuttapongB/Desktop/tableau-agent-toolkit" && python -m pytest tests/integration/test_cli.py -x -q</automated>
  </verify>
  <done>
    - validate-semantic command registered and working
    - qa static command registered and working (via qa subcommand group)
    - validate-semantic exits 0 for valid TWB, exits 1 for broken TWB
    - qa static prints markdown to stdout by default
    - qa static --output writes markdown file to specified path
    - qa static exits 1 when any check fails
    - All 11 new integration tests passing
    - Both commands visible in --help output
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> CLI arguments | User provides file paths as CLI arguments |
| CLI -> filesystem | CLI reads TWB files and writes report files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | T | CLI file path arguments | mitigate | Typer's exists=True validates file existence before handler runs; no path traversal risk since we only read/write explicit paths |
| T-02-09 | I | CLI output (stdout/file) | accept | Output is markdown report, not executable; no injection risk |
</threat_model>

<verification>
- All CLI integration tests pass: `pytest tests/integration/test_cli.py -x -q`
- validate-semantic command discoverable: `python -m tableau_agent_toolkit.cli --help | grep validate-semantic`
- qa static command discoverable: `python -m tableau_agent_toolkit.cli qa --help | grep static`
- Full test suite green: `pytest tests/ -x -q`
</verification>

<success_criteria>
- validate-semantic command works end-to-end: valid TWB exits 0, broken TWB exits 1 with error messages
- qa static command works end-to-end: produces markdown report, --output writes to file
- Both commands visible in --help
- All 11 new integration tests passing alongside existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-and-qa/02-04-SUMMARY.md`
</output>
